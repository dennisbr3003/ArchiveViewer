<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ZipApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@drawable/binary"
        android:label="@string/app_name"
        android:roundIcon="@drawable/binary"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyPackedFileViewer"
        tools:targetApi="31">
        <activity
            android:name=".ImageLoginActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            tools:ignore="DiscouragedApi,LockedOrientationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ZipContentViewerActivity"
            android:exported="false" />
        <activity
            android:name=".ZipFileActivity"
            android:exported="false" />
        <activity
            android:name=".ZipLibraryActivity"
            android:exported="false" />
    </application>

</manifest>